{
    "name": "esa/robot-coding-challenge",
    "description": "Python ESA robot coding challenge with PHPUnit functional tests.",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5 || ^11.0"
    },
    "scripts": {
        "test:php": "phpunit --configuration phpunit.xml"
    },
    "config": {
        "sort-packages": true
    }
}

